<?php

/**
 * Base class that represents a row from the 'film' table.
 *
 * 
 *
 * This class was autogenerated by Propel 1.4.0 on:
 *
 * Thu Jan 28 23:14:34 2010
 *
 * @package    lib.model.om
 */
abstract class BaseFilm extends BaseObject  implements Persistent {


	/**
	 * The Peer class.
	 * Instance provides a convenient way of calling static methods on a class
	 * that calling code may not be able to identify.
	 * @var        FilmPeer
	 */
	protected static $peer;

	/**
	 * The value for the id field.
	 * @var        int
	 */
	protected $id;

	/**
	 * The value for the user_id field.
	 * @var        int
	 */
	protected $user_id;

	/**
	 * The value for the title field.
	 * @var        string
	 */
	protected $title;

	/**
	 * The value for the original_title field.
	 * @var        string
	 */
	protected $original_title;

	/**
	 * The value for the normal_logo field.
	 * @var        string
	 */
	protected $normal_logo;

	/**
	 * The value for the thumb_logo field.
	 * @var        string
	 */
	protected $thumb_logo;

	/**
	 * The value for the url field.
	 * @var        string
	 */
	protected $url;

	/**
	 * The value for the pub_year field.
	 * @var        int
	 */
	protected $pub_year;

	/**
	 * The value for the director field.
	 * @var        string
	 */
	protected $director;

	/**
	 * The value for the cast field.
	 * @var        string
	 */
	protected $cast;

	/**
	 * The value for the about field.
	 * @var        string
	 */
	protected $about;

	/**
	 * The value for the country field.
	 * @var        string
	 */
	protected $country;

	/**
	 * The value for the duration field.
	 * @var        string
	 */
	protected $duration;

	/**
	 * The value for the file_info field.
	 * @var        string
	 */
	protected $file_info;

	/**
	 * The value for the is_visible field.
	 * Note: this column has a database default value of: true
	 * @var        boolean
	 */
	protected $is_visible;

	/**
	 * The value for the is_private field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_private;

	/**
	 * The value for the is_public field.
	 * Note: this column has a database default value of: false
	 * @var        boolean
	 */
	protected $is_public;

	/**
	 * The value for the modified_user_id field.
	 * @var        int
	 */
	protected $modified_user_id;

	/**
	 * The value for the modified_at field.
	 * @var        string
	 */
	protected $modified_at;

	/**
	 * The value for the modified_text field.
	 * @var        string
	 */
	protected $modified_text;

	/**
	 * The value for the created_at field.
	 * @var        string
	 */
	protected $created_at;

	/**
	 * The value for the updated_at field.
	 * @var        string
	 */
	protected $updated_at;

	/**
	 * @var        Users
	 */
	protected $aUsersRelatedByUserId;

	/**
	 * @var        Users
	 */
	protected $aUsersRelatedByModifiedUserId;

	/**
	 * @var        array FilmLinks[] Collection to store aggregation of FilmLinks objects.
	 */
	protected $collFilmLinkss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFilmLinkss.
	 */
	private $lastFilmLinksCriteria = null;

	/**
	 * @var        array FilmRaiting[] Collection to store aggregation of FilmRaiting objects.
	 */
	protected $collFilmRaitings;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFilmRaitings.
	 */
	private $lastFilmRaitingCriteria = null;

	/**
	 * @var        array FilmTotalRating[] Collection to store aggregation of FilmTotalRating objects.
	 */
	protected $collFilmTotalRatings;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFilmTotalRatings.
	 */
	private $lastFilmTotalRatingCriteria = null;

	/**
	 * @var        array FilmGallery[] Collection to store aggregation of FilmGallery objects.
	 */
	protected $collFilmGallerys;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFilmGallerys.
	 */
	private $lastFilmGalleryCriteria = null;

	/**
	 * @var        array FilmFilmTypes[] Collection to store aggregation of FilmFilmTypes objects.
	 */
	protected $collFilmFilmTypess;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFilmFilmTypess.
	 */
	private $lastFilmFilmTypesCriteria = null;

	/**
	 * @var        array FilmTrailer[] Collection to store aggregation of FilmTrailer objects.
	 */
	protected $collFilmTrailers;

	/**
	 * @var        Criteria The criteria used to select the current contents of collFilmTrailers.
	 */
	private $lastFilmTrailerCriteria = null;

	/**
	 * @var        array Comments[] Collection to store aggregation of Comments objects.
	 */
	protected $collCommentss;

	/**
	 * @var        Criteria The criteria used to select the current contents of collCommentss.
	 */
	private $lastCommentsCriteria = null;

	/**
	 * Flag to prevent endless save loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInSave = false;

	/**
	 * Flag to prevent endless validation loop, if this object is referenced
	 * by another object which falls in this transaction.
	 * @var        boolean
	 */
	protected $alreadyInValidation = false;

	// symfony behavior
	
	const PEER = 'FilmPeer';

	/**
	 * Applies default values to this object.
	 * This method should be called from the object's constructor (or
	 * equivalent initialization method).
	 * @see        __construct()
	 */
	public function applyDefaultValues()
	{
		$this->is_visible = true;
		$this->is_private = false;
		$this->is_public = false;
	}

	/**
	 * Initializes internal state of BaseFilm object.
	 * @see        applyDefaults()
	 */
	public function __construct()
	{
		parent::__construct();
		$this->applyDefaultValues();
	}

	/**
	 * Get the [id] column value.
	 * 
	 * @return     int
	 */
	public function getId()
	{
		return $this->id;
	}

	/**
	 * Get the [user_id] column value.
	 * 
	 * @return     int
	 */
	public function getUserId()
	{
		return $this->user_id;
	}

	/**
	 * Get the [title] column value.
	 * 
	 * @return     string
	 */
	public function getTitle()
	{
		return $this->title;
	}

	/**
	 * Get the [original_title] column value.
	 * 
	 * @return     string
	 */
	public function getOriginalTitle()
	{
		return $this->original_title;
	}

	/**
	 * Get the [normal_logo] column value.
	 * 
	 * @return     string
	 */
	public function getNormalLogo()
	{
		return $this->normal_logo;
	}

	/**
	 * Get the [thumb_logo] column value.
	 * 
	 * @return     string
	 */
	public function getThumbLogo()
	{
		return $this->thumb_logo;
	}

	/**
	 * Get the [url] column value.
	 * 
	 * @return     string
	 */
	public function getUrl()
	{
		return $this->url;
	}

	/**
	 * Get the [pub_year] column value.
	 * 
	 * @return     int
	 */
	public function getPubYear()
	{
		return $this->pub_year;
	}

	/**
	 * Get the [director] column value.
	 * 
	 * @return     string
	 */
	public function getDirector()
	{
		return $this->director;
	}

	/**
	 * Get the [cast] column value.
	 * 
	 * @return     string
	 */
	public function getCast()
	{
		return $this->cast;
	}

	/**
	 * Get the [about] column value.
	 * 
	 * @return     string
	 */
	public function getAbout()
	{
		return $this->about;
	}

	/**
	 * Get the [country] column value.
	 * 
	 * @return     string
	 */
	public function getCountry()
	{
		return $this->country;
	}

	/**
	 * Get the [duration] column value.
	 * 
	 * @return     string
	 */
	public function getDuration()
	{
		return $this->duration;
	}

	/**
	 * Get the [file_info] column value.
	 * 
	 * @return     string
	 */
	public function getFileInfo()
	{
		return $this->file_info;
	}

	/**
	 * Get the [is_visible] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsVisible()
	{
		return $this->is_visible;
	}

	/**
	 * Get the [is_private] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsPrivate()
	{
		return $this->is_private;
	}

	/**
	 * Get the [is_public] column value.
	 * 
	 * @return     boolean
	 */
	public function getIsPublic()
	{
		return $this->is_public;
	}

	/**
	 * Get the [modified_user_id] column value.
	 * 
	 * @return     int
	 */
	public function getModifiedUserId()
	{
		return $this->modified_user_id;
	}

	/**
	 * Get the [optionally formatted] temporal [modified_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getModifiedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->modified_at === null) {
			return null;
		}


		if ($this->modified_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->modified_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->modified_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [modified_text] column value.
	 * 
	 * @return     string
	 */
	public function getModifiedText()
	{
		return $this->modified_text;
	}

	/**
	 * Get the [optionally formatted] temporal [created_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getCreatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->created_at === null) {
			return null;
		}


		if ($this->created_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->created_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->created_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Get the [optionally formatted] temporal [updated_at] column value.
	 * 
	 *
	 * @param      string $format The date/time format string (either date()-style or strftime()-style).
	 *							If format is NULL, then the raw DateTime object will be returned.
	 * @return     mixed Formatted date/time value as string or DateTime object (if format is NULL), NULL if column is NULL, and 0 if column value is 0000-00-00 00:00:00
	 * @throws     PropelException - if unable to parse/validate the date/time value.
	 */
	public function getUpdatedAt($format = 'Y-m-d H:i:s')
	{
		if ($this->updated_at === null) {
			return null;
		}


		if ($this->updated_at === '0000-00-00 00:00:00') {
			// while technically this is not a default value of NULL,
			// this seems to be closest in meaning.
			return null;
		} else {
			try {
				$dt = new DateTime($this->updated_at);
			} catch (Exception $x) {
				throw new PropelException("Internally stored date/time/timestamp value could not be converted to DateTime: " . var_export($this->updated_at, true), $x);
			}
		}

		if ($format === null) {
			// Because propel.useDateTimeClass is TRUE, we return a DateTime object.
			return $dt;
		} elseif (strpos($format, '%') !== false) {
			return strftime($format, $dt->format('U'));
		} else {
			return $dt->format($format);
		}
	}

	/**
	 * Set the value of [id] column.
	 * 
	 * @param      int $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->id !== $v) {
			$this->id = $v;
			$this->modifiedColumns[] = FilmPeer::ID;
		}

		return $this;
	} // setId()

	/**
	 * Set the value of [user_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setUserId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->user_id !== $v) {
			$this->user_id = $v;
			$this->modifiedColumns[] = FilmPeer::USER_ID;
		}

		if ($this->aUsersRelatedByUserId !== null && $this->aUsersRelatedByUserId->getId() !== $v) {
			$this->aUsersRelatedByUserId = null;
		}

		return $this;
	} // setUserId()

	/**
	 * Set the value of [title] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setTitle($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->title !== $v) {
			$this->title = $v;
			$this->modifiedColumns[] = FilmPeer::TITLE;
		}

		return $this;
	} // setTitle()

	/**
	 * Set the value of [original_title] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setOriginalTitle($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->original_title !== $v) {
			$this->original_title = $v;
			$this->modifiedColumns[] = FilmPeer::ORIGINAL_TITLE;
		}

		return $this;
	} // setOriginalTitle()

	/**
	 * Set the value of [normal_logo] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setNormalLogo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->normal_logo !== $v) {
			$this->normal_logo = $v;
			$this->modifiedColumns[] = FilmPeer::NORMAL_LOGO;
		}

		return $this;
	} // setNormalLogo()

	/**
	 * Set the value of [thumb_logo] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setThumbLogo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->thumb_logo !== $v) {
			$this->thumb_logo = $v;
			$this->modifiedColumns[] = FilmPeer::THUMB_LOGO;
		}

		return $this;
	} // setThumbLogo()

	/**
	 * Set the value of [url] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setUrl($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->url !== $v) {
			$this->url = $v;
			$this->modifiedColumns[] = FilmPeer::URL;
		}

		return $this;
	} // setUrl()

	/**
	 * Set the value of [pub_year] column.
	 * 
	 * @param      int $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setPubYear($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->pub_year !== $v) {
			$this->pub_year = $v;
			$this->modifiedColumns[] = FilmPeer::PUB_YEAR;
		}

		return $this;
	} // setPubYear()

	/**
	 * Set the value of [director] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setDirector($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->director !== $v) {
			$this->director = $v;
			$this->modifiedColumns[] = FilmPeer::DIRECTOR;
		}

		return $this;
	} // setDirector()

	/**
	 * Set the value of [cast] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setCast($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->cast !== $v) {
			$this->cast = $v;
			$this->modifiedColumns[] = FilmPeer::CAST;
		}

		return $this;
	} // setCast()

	/**
	 * Set the value of [about] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setAbout($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->about !== $v) {
			$this->about = $v;
			$this->modifiedColumns[] = FilmPeer::ABOUT;
		}

		return $this;
	} // setAbout()

	/**
	 * Set the value of [country] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setCountry($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->country !== $v) {
			$this->country = $v;
			$this->modifiedColumns[] = FilmPeer::COUNTRY;
		}

		return $this;
	} // setCountry()

	/**
	 * Set the value of [duration] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setDuration($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->duration !== $v) {
			$this->duration = $v;
			$this->modifiedColumns[] = FilmPeer::DURATION;
		}

		return $this;
	} // setDuration()

	/**
	 * Set the value of [file_info] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setFileInfo($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->file_info !== $v) {
			$this->file_info = $v;
			$this->modifiedColumns[] = FilmPeer::FILE_INFO;
		}

		return $this;
	} // setFileInfo()

	/**
	 * Set the value of [is_visible] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setIsVisible($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_visible !== $v || $this->isNew()) {
			$this->is_visible = $v;
			$this->modifiedColumns[] = FilmPeer::IS_VISIBLE;
		}

		return $this;
	} // setIsVisible()

	/**
	 * Set the value of [is_private] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setIsPrivate($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_private !== $v || $this->isNew()) {
			$this->is_private = $v;
			$this->modifiedColumns[] = FilmPeer::IS_PRIVATE;
		}

		return $this;
	} // setIsPrivate()

	/**
	 * Set the value of [is_public] column.
	 * 
	 * @param      boolean $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setIsPublic($v)
	{
		if ($v !== null) {
			$v = (boolean) $v;
		}

		if ($this->is_public !== $v || $this->isNew()) {
			$this->is_public = $v;
			$this->modifiedColumns[] = FilmPeer::IS_PUBLIC;
		}

		return $this;
	} // setIsPublic()

	/**
	 * Set the value of [modified_user_id] column.
	 * 
	 * @param      int $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setModifiedUserId($v)
	{
		if ($v !== null) {
			$v = (int) $v;
		}

		if ($this->modified_user_id !== $v) {
			$this->modified_user_id = $v;
			$this->modifiedColumns[] = FilmPeer::MODIFIED_USER_ID;
		}

		if ($this->aUsersRelatedByModifiedUserId !== null && $this->aUsersRelatedByModifiedUserId->getId() !== $v) {
			$this->aUsersRelatedByModifiedUserId = null;
		}

		return $this;
	} // setModifiedUserId()

	/**
	 * Sets the value of [modified_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Film The current object (for fluent API support)
	 */
	public function setModifiedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->modified_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->modified_at !== null && $tmpDt = new DateTime($this->modified_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->modified_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = FilmPeer::MODIFIED_AT;
			}
		} // if either are not null

		return $this;
	} // setModifiedAt()

	/**
	 * Set the value of [modified_text] column.
	 * 
	 * @param      string $v new value
	 * @return     Film The current object (for fluent API support)
	 */
	public function setModifiedText($v)
	{
		if ($v !== null) {
			$v = (string) $v;
		}

		if ($this->modified_text !== $v) {
			$this->modified_text = $v;
			$this->modifiedColumns[] = FilmPeer::MODIFIED_TEXT;
		}

		return $this;
	} // setModifiedText()

	/**
	 * Sets the value of [created_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Film The current object (for fluent API support)
	 */
	public function setCreatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->created_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->created_at !== null && $tmpDt = new DateTime($this->created_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->created_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = FilmPeer::CREATED_AT;
			}
		} // if either are not null

		return $this;
	} // setCreatedAt()

	/**
	 * Sets the value of [updated_at] column to a normalized version of the date/time value specified.
	 * 
	 * @param      mixed $v string, integer (timestamp), or DateTime value.  Empty string will
	 *						be treated as NULL for temporal objects.
	 * @return     Film The current object (for fluent API support)
	 */
	public function setUpdatedAt($v)
	{
		// we treat '' as NULL for temporal objects because DateTime('') == DateTime('now')
		// -- which is unexpected, to say the least.
		if ($v === null || $v === '') {
			$dt = null;
		} elseif ($v instanceof DateTime) {
			$dt = $v;
		} else {
			// some string/numeric value passed; we normalize that so that we can
			// validate it.
			try {
				if (is_numeric($v)) { // if it's a unix timestamp
					$dt = new DateTime('@'.$v, new DateTimeZone('UTC'));
					// We have to explicitly specify and then change the time zone because of a
					// DateTime bug: http://bugs.php.net/bug.php?id=43003
					$dt->setTimeZone(new DateTimeZone(date_default_timezone_get()));
				} else {
					$dt = new DateTime($v);
				}
			} catch (Exception $x) {
				throw new PropelException('Error parsing date/time value: ' . var_export($v, true), $x);
			}
		}

		if ( $this->updated_at !== null || $dt !== null ) {
			// (nested ifs are a little easier to read in this case)

			$currNorm = ($this->updated_at !== null && $tmpDt = new DateTime($this->updated_at)) ? $tmpDt->format('Y-m-d H:i:s') : null;
			$newNorm = ($dt !== null) ? $dt->format('Y-m-d H:i:s') : null;

			if ( ($currNorm !== $newNorm) // normalized values don't match 
					)
			{
				$this->updated_at = ($dt ? $dt->format('Y-m-d H:i:s') : null);
				$this->modifiedColumns[] = FilmPeer::UPDATED_AT;
			}
		} // if either are not null

		return $this;
	} // setUpdatedAt()

	/**
	 * Indicates whether the columns in this object are only set to default values.
	 *
	 * This method can be used in conjunction with isModified() to indicate whether an object is both
	 * modified _and_ has some values set which are non-default.
	 *
	 * @return     boolean Whether the columns in this object are only been set with default values.
	 */
	public function hasOnlyDefaultValues()
	{
			if ($this->is_visible !== true) {
				return false;
			}

			if ($this->is_private !== false) {
				return false;
			}

			if ($this->is_public !== false) {
				return false;
			}

		// otherwise, everything was equal, so return TRUE
		return true;
	} // hasOnlyDefaultValues()

	/**
	 * Hydrates (populates) the object variables with values from the database resultset.
	 *
	 * An offset (0-based "start column") is specified so that objects can be hydrated
	 * with a subset of the columns in the resultset rows.  This is needed, for example,
	 * for results of JOIN queries where the resultset row includes columns from two or
	 * more tables.
	 *
	 * @param      array $row The row returned by PDOStatement->fetch(PDO::FETCH_NUM)
	 * @param      int $startcol 0-based offset column which indicates which restultset column to start with.
	 * @param      boolean $rehydrate Whether this object is being re-hydrated from the database.
	 * @return     int next starting column
	 * @throws     PropelException  - Any caught Exception will be rewrapped as a PropelException.
	 */
	public function hydrate($row, $startcol = 0, $rehydrate = false)
	{
		try {

			$this->id = ($row[$startcol + 0] !== null) ? (int) $row[$startcol + 0] : null;
			$this->user_id = ($row[$startcol + 1] !== null) ? (int) $row[$startcol + 1] : null;
			$this->title = ($row[$startcol + 2] !== null) ? (string) $row[$startcol + 2] : null;
			$this->original_title = ($row[$startcol + 3] !== null) ? (string) $row[$startcol + 3] : null;
			$this->normal_logo = ($row[$startcol + 4] !== null) ? (string) $row[$startcol + 4] : null;
			$this->thumb_logo = ($row[$startcol + 5] !== null) ? (string) $row[$startcol + 5] : null;
			$this->url = ($row[$startcol + 6] !== null) ? (string) $row[$startcol + 6] : null;
			$this->pub_year = ($row[$startcol + 7] !== null) ? (int) $row[$startcol + 7] : null;
			$this->director = ($row[$startcol + 8] !== null) ? (string) $row[$startcol + 8] : null;
			$this->cast = ($row[$startcol + 9] !== null) ? (string) $row[$startcol + 9] : null;
			$this->about = ($row[$startcol + 10] !== null) ? (string) $row[$startcol + 10] : null;
			$this->country = ($row[$startcol + 11] !== null) ? (string) $row[$startcol + 11] : null;
			$this->duration = ($row[$startcol + 12] !== null) ? (string) $row[$startcol + 12] : null;
			$this->file_info = ($row[$startcol + 13] !== null) ? (string) $row[$startcol + 13] : null;
			$this->is_visible = ($row[$startcol + 14] !== null) ? (boolean) $row[$startcol + 14] : null;
			$this->is_private = ($row[$startcol + 15] !== null) ? (boolean) $row[$startcol + 15] : null;
			$this->is_public = ($row[$startcol + 16] !== null) ? (boolean) $row[$startcol + 16] : null;
			$this->modified_user_id = ($row[$startcol + 17] !== null) ? (int) $row[$startcol + 17] : null;
			$this->modified_at = ($row[$startcol + 18] !== null) ? (string) $row[$startcol + 18] : null;
			$this->modified_text = ($row[$startcol + 19] !== null) ? (string) $row[$startcol + 19] : null;
			$this->created_at = ($row[$startcol + 20] !== null) ? (string) $row[$startcol + 20] : null;
			$this->updated_at = ($row[$startcol + 21] !== null) ? (string) $row[$startcol + 21] : null;
			$this->resetModified();

			$this->setNew(false);

			if ($rehydrate) {
				$this->ensureConsistency();
			}

			// FIXME - using NUM_COLUMNS may be clearer.
			return $startcol + 22; // 22 = FilmPeer::NUM_COLUMNS - FilmPeer::NUM_LAZY_LOAD_COLUMNS).

		} catch (Exception $e) {
			throw new PropelException("Error populating Film object", $e);
		}
	}

	/**
	 * Checks and repairs the internal consistency of the object.
	 *
	 * This method is executed after an already-instantiated object is re-hydrated
	 * from the database.  It exists to check any foreign keys to make sure that
	 * the objects related to the current object are correct based on foreign key.
	 *
	 * You can override this method in the stub class, but you should always invoke
	 * the base method from the overridden method (i.e. parent::ensureConsistency()),
	 * in case your model changes.
	 *
	 * @throws     PropelException
	 */
	public function ensureConsistency()
	{

		if ($this->aUsersRelatedByUserId !== null && $this->user_id !== $this->aUsersRelatedByUserId->getId()) {
			$this->aUsersRelatedByUserId = null;
		}
		if ($this->aUsersRelatedByModifiedUserId !== null && $this->modified_user_id !== $this->aUsersRelatedByModifiedUserId->getId()) {
			$this->aUsersRelatedByModifiedUserId = null;
		}
	} // ensureConsistency

	/**
	 * Reloads this object from datastore based on primary key and (optionally) resets all associated objects.
	 *
	 * This will only work if the object has been saved and has a valid primary key set.
	 *
	 * @param      boolean $deep (optional) Whether to also de-associated any related objects.
	 * @param      PropelPDO $con (optional) The PropelPDO connection to use.
	 * @return     void
	 * @throws     PropelException - if this object is deleted, unsaved or doesn't have pk match in db
	 */
	public function reload($deep = false, PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("Cannot reload a deleted object.");
		}

		if ($this->isNew()) {
			throw new PropelException("Cannot reload an unsaved object.");
		}

		if ($con === null) {
			$con = Propel::getConnection(FilmPeer::DATABASE_NAME, Propel::CONNECTION_READ);
		}

		// We don't need to alter the object instance pool; we're just modifying this instance
		// already in the pool.

		$stmt = FilmPeer::doSelectStmt($this->buildPkeyCriteria(), $con);
		$row = $stmt->fetch(PDO::FETCH_NUM);
		$stmt->closeCursor();
		if (!$row) {
			throw new PropelException('Cannot find matching row in the database to reload object values.');
		}
		$this->hydrate($row, 0, true); // rehydrate

		if ($deep) {  // also de-associate any related objects?

			$this->aUsersRelatedByUserId = null;
			$this->aUsersRelatedByModifiedUserId = null;
			$this->collFilmLinkss = null;
			$this->lastFilmLinksCriteria = null;

			$this->collFilmRaitings = null;
			$this->lastFilmRaitingCriteria = null;

			$this->collFilmTotalRatings = null;
			$this->lastFilmTotalRatingCriteria = null;

			$this->collFilmGallerys = null;
			$this->lastFilmGalleryCriteria = null;

			$this->collFilmFilmTypess = null;
			$this->lastFilmFilmTypesCriteria = null;

			$this->collFilmTrailers = null;
			$this->lastFilmTrailerCriteria = null;

			$this->collCommentss = null;
			$this->lastCommentsCriteria = null;

		} // if (deep)
	}

	/**
	 * Removes this object from datastore and sets delete attribute.
	 *
	 * @param      PropelPDO $con
	 * @return     void
	 * @throws     PropelException
	 * @see        BaseObject::setDeleted()
	 * @see        BaseObject::isDeleted()
	 */
	public function delete(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("This object has already been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(FilmPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		try {
			$ret = $this->preDelete($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseFilm:delete:pre') as $callable)
			{
			  if ($ret = call_user_func($callable, $this, $con))
			  {
			    return;
			  }
			}

			if ($ret) {
				FilmPeer::doDelete($this, $con);
				$this->postDelete($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseFilm:delete:post') as $callable)
				{
				  call_user_func($callable, $this, $con);
				}

				$this->setDeleted(true);
				$con->commit();
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Persists this object to the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All modified related objects will also be persisted in the doSave()
	 * method.  This method wraps all precipitate database operations in a
	 * single transaction.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        doSave()
	 */
	public function save(PropelPDO $con = null)
	{
		if ($this->isDeleted()) {
			throw new PropelException("You cannot save an object that has been deleted.");
		}

		if ($con === null) {
			$con = Propel::getConnection(FilmPeer::DATABASE_NAME, Propel::CONNECTION_WRITE);
		}
		
		$con->beginTransaction();
		$isInsert = $this->isNew();
		try {
			$ret = $this->preSave($con);
			// symfony_behaviors behavior
			foreach (sfMixer::getCallables('BaseFilm:save:pre') as $callable)
			{
			  if (is_integer($affectedRows = call_user_func($callable, $this, $con)))
			  {
			    return $affectedRows;
			  }
			}

			// symfony_timestampable behavior
			if ($this->isModified() && !$this->isColumnModified(FilmPeer::UPDATED_AT))
			{
			  $this->setUpdatedAt(time());
			}

			if ($isInsert) {
				$ret = $ret && $this->preInsert($con);
				// symfony_timestampable behavior
				if (!$this->isColumnModified(FilmPeer::CREATED_AT))
				{
				  $this->setCreatedAt(time());
				}

			} else {
				$ret = $ret && $this->preUpdate($con);
			}
			if ($ret) {
				$affectedRows = $this->doSave($con);
				if ($isInsert) {
					$this->postInsert($con);
				} else {
					$this->postUpdate($con);
				}
				$this->postSave($con);
				// symfony_behaviors behavior
				foreach (sfMixer::getCallables('BaseFilm:save:post') as $callable)
				{
				  call_user_func($callable, $this, $con, $affectedRows);
				}

				$con->commit();
				FilmPeer::addInstanceToPool($this);
				return $affectedRows;
			}
		} catch (PropelException $e) {
			$con->rollBack();
			throw $e;
		}
	}

	/**
	 * Performs the work of inserting or updating the row in the database.
	 *
	 * If the object is new, it inserts it; otherwise an update is performed.
	 * All related objects are also updated in this method.
	 *
	 * @param      PropelPDO $con
	 * @return     int The number of rows affected by this insert/update and any referring fk objects' save() operations.
	 * @throws     PropelException
	 * @see        save()
	 */
	protected function doSave(PropelPDO $con)
	{
		$affectedRows = 0; // initialize var to track total num of affected rows
		if (!$this->alreadyInSave) {
			$this->alreadyInSave = true;

			// We call the save method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aUsersRelatedByUserId !== null) {
				if ($this->aUsersRelatedByUserId->isModified() || $this->aUsersRelatedByUserId->isNew()) {
					$affectedRows += $this->aUsersRelatedByUserId->save($con);
				}
				$this->setUsersRelatedByUserId($this->aUsersRelatedByUserId);
			}

			if ($this->aUsersRelatedByModifiedUserId !== null) {
				if ($this->aUsersRelatedByModifiedUserId->isModified() || $this->aUsersRelatedByModifiedUserId->isNew()) {
					$affectedRows += $this->aUsersRelatedByModifiedUserId->save($con);
				}
				$this->setUsersRelatedByModifiedUserId($this->aUsersRelatedByModifiedUserId);
			}

			if ($this->isNew() ) {
				$this->modifiedColumns[] = FilmPeer::ID;
			}

			// If this object has been modified, then save it to the database.
			if ($this->isModified()) {
				if ($this->isNew()) {
					$pk = FilmPeer::doInsert($this, $con);
					$affectedRows += 1; // we are assuming that there is only 1 row per doInsert() which
										 // should always be true here (even though technically
										 // BasePeer::doInsert() can insert multiple rows).

					$this->setId($pk);  //[IMV] update autoincrement primary key

					$this->setNew(false);
				} else {
					$affectedRows += FilmPeer::doUpdate($this, $con);
				}

				$this->resetModified(); // [HL] After being saved an object is no longer 'modified'
			}

			if ($this->collFilmLinkss !== null) {
				foreach ($this->collFilmLinkss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFilmRaitings !== null) {
				foreach ($this->collFilmRaitings as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFilmTotalRatings !== null) {
				foreach ($this->collFilmTotalRatings as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFilmGallerys !== null) {
				foreach ($this->collFilmGallerys as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFilmFilmTypess !== null) {
				foreach ($this->collFilmFilmTypess as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collFilmTrailers !== null) {
				foreach ($this->collFilmTrailers as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			if ($this->collCommentss !== null) {
				foreach ($this->collCommentss as $referrerFK) {
					if (!$referrerFK->isDeleted()) {
						$affectedRows += $referrerFK->save($con);
					}
				}
			}

			$this->alreadyInSave = false;

		}
		return $affectedRows;
	} // doSave()

	/**
	 * Array of ValidationFailed objects.
	 * @var        array ValidationFailed[]
	 */
	protected $validationFailures = array();

	/**
	 * Gets any ValidationFailed objects that resulted from last call to validate().
	 *
	 *
	 * @return     array ValidationFailed[]
	 * @see        validate()
	 */
	public function getValidationFailures()
	{
		return $this->validationFailures;
	}

	/**
	 * Validates the objects modified field values and all objects related to this table.
	 *
	 * If $columns is either a column name or an array of column names
	 * only those columns are validated.
	 *
	 * @param      mixed $columns Column name or an array of column names.
	 * @return     boolean Whether all columns pass validation.
	 * @see        doValidate()
	 * @see        getValidationFailures()
	 */
	public function validate($columns = null)
	{
		$res = $this->doValidate($columns);
		if ($res === true) {
			$this->validationFailures = array();
			return true;
		} else {
			$this->validationFailures = $res;
			return false;
		}
	}

	/**
	 * This function performs the validation work for complex object models.
	 *
	 * In addition to checking the current object, all related objects will
	 * also be validated.  If all pass then <code>true</code> is returned; otherwise
	 * an aggreagated array of ValidationFailed objects will be returned.
	 *
	 * @param      array $columns Array of column names to validate.
	 * @return     mixed <code>true</code> if all validations pass; array of <code>ValidationFailed</code> objets otherwise.
	 */
	protected function doValidate($columns = null)
	{
		if (!$this->alreadyInValidation) {
			$this->alreadyInValidation = true;
			$retval = null;

			$failureMap = array();


			// We call the validate method on the following object(s) if they
			// were passed to this object by their coresponding set
			// method.  This object relates to these object(s) by a
			// foreign key reference.

			if ($this->aUsersRelatedByUserId !== null) {
				if (!$this->aUsersRelatedByUserId->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUsersRelatedByUserId->getValidationFailures());
				}
			}

			if ($this->aUsersRelatedByModifiedUserId !== null) {
				if (!$this->aUsersRelatedByModifiedUserId->validate($columns)) {
					$failureMap = array_merge($failureMap, $this->aUsersRelatedByModifiedUserId->getValidationFailures());
				}
			}


			if (($retval = FilmPeer::doValidate($this, $columns)) !== true) {
				$failureMap = array_merge($failureMap, $retval);
			}


				if ($this->collFilmLinkss !== null) {
					foreach ($this->collFilmLinkss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFilmRaitings !== null) {
					foreach ($this->collFilmRaitings as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFilmTotalRatings !== null) {
					foreach ($this->collFilmTotalRatings as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFilmGallerys !== null) {
					foreach ($this->collFilmGallerys as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFilmFilmTypess !== null) {
					foreach ($this->collFilmFilmTypess as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collFilmTrailers !== null) {
					foreach ($this->collFilmTrailers as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}

				if ($this->collCommentss !== null) {
					foreach ($this->collCommentss as $referrerFK) {
						if (!$referrerFK->validate($columns)) {
							$failureMap = array_merge($failureMap, $referrerFK->getValidationFailures());
						}
					}
				}


			$this->alreadyInValidation = false;
		}

		return (!empty($failureMap) ? $failureMap : true);
	}

	/**
	 * Retrieves a field from the object by name passed in as a string.
	 *
	 * @param      string $name name
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     mixed Value of field.
	 */
	public function getByName($name, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = FilmPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		$field = $this->getByPosition($pos);
		return $field;
	}

	/**
	 * Retrieves a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @return     mixed Value of field at $pos
	 */
	public function getByPosition($pos)
	{
		switch($pos) {
			case 0:
				return $this->getId();
				break;
			case 1:
				return $this->getUserId();
				break;
			case 2:
				return $this->getTitle();
				break;
			case 3:
				return $this->getOriginalTitle();
				break;
			case 4:
				return $this->getNormalLogo();
				break;
			case 5:
				return $this->getThumbLogo();
				break;
			case 6:
				return $this->getUrl();
				break;
			case 7:
				return $this->getPubYear();
				break;
			case 8:
				return $this->getDirector();
				break;
			case 9:
				return $this->getCast();
				break;
			case 10:
				return $this->getAbout();
				break;
			case 11:
				return $this->getCountry();
				break;
			case 12:
				return $this->getDuration();
				break;
			case 13:
				return $this->getFileInfo();
				break;
			case 14:
				return $this->getIsVisible();
				break;
			case 15:
				return $this->getIsPrivate();
				break;
			case 16:
				return $this->getIsPublic();
				break;
			case 17:
				return $this->getModifiedUserId();
				break;
			case 18:
				return $this->getModifiedAt();
				break;
			case 19:
				return $this->getModifiedText();
				break;
			case 20:
				return $this->getCreatedAt();
				break;
			case 21:
				return $this->getUpdatedAt();
				break;
			default:
				return null;
				break;
		} // switch()
	}

	/**
	 * Exports the object as an array.
	 *
	 * You can specify the key type of the array by passing one of the class
	 * type constants.
	 *
	 * @param      string $keyType (optional) One of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                        BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM. Defaults to BasePeer::TYPE_PHPNAME.
	 * @param      boolean $includeLazyLoadColumns (optional) Whether to include lazy loaded columns.  Defaults to TRUE.
	 * @return     an associative array containing the field names (as keys) and field values
	 */
	public function toArray($keyType = BasePeer::TYPE_PHPNAME, $includeLazyLoadColumns = true)
	{
		$keys = FilmPeer::getFieldNames($keyType);
		$result = array(
			$keys[0] => $this->getId(),
			$keys[1] => $this->getUserId(),
			$keys[2] => $this->getTitle(),
			$keys[3] => $this->getOriginalTitle(),
			$keys[4] => $this->getNormalLogo(),
			$keys[5] => $this->getThumbLogo(),
			$keys[6] => $this->getUrl(),
			$keys[7] => $this->getPubYear(),
			$keys[8] => $this->getDirector(),
			$keys[9] => $this->getCast(),
			$keys[10] => $this->getAbout(),
			$keys[11] => $this->getCountry(),
			$keys[12] => $this->getDuration(),
			$keys[13] => $this->getFileInfo(),
			$keys[14] => $this->getIsVisible(),
			$keys[15] => $this->getIsPrivate(),
			$keys[16] => $this->getIsPublic(),
			$keys[17] => $this->getModifiedUserId(),
			$keys[18] => $this->getModifiedAt(),
			$keys[19] => $this->getModifiedText(),
			$keys[20] => $this->getCreatedAt(),
			$keys[21] => $this->getUpdatedAt(),
		);
		return $result;
	}

	/**
	 * Sets a field from the object by name passed in as a string.
	 *
	 * @param      string $name peer name
	 * @param      mixed $value field value
	 * @param      string $type The type of fieldname the $name is of:
	 *                     one of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME
	 *                     BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM
	 * @return     void
	 */
	public function setByName($name, $value, $type = BasePeer::TYPE_PHPNAME)
	{
		$pos = FilmPeer::translateFieldName($name, $type, BasePeer::TYPE_NUM);
		return $this->setByPosition($pos, $value);
	}

	/**
	 * Sets a field from the object by Position as specified in the xml schema.
	 * Zero-based.
	 *
	 * @param      int $pos position in xml schema
	 * @param      mixed $value field value
	 * @return     void
	 */
	public function setByPosition($pos, $value)
	{
		switch($pos) {
			case 0:
				$this->setId($value);
				break;
			case 1:
				$this->setUserId($value);
				break;
			case 2:
				$this->setTitle($value);
				break;
			case 3:
				$this->setOriginalTitle($value);
				break;
			case 4:
				$this->setNormalLogo($value);
				break;
			case 5:
				$this->setThumbLogo($value);
				break;
			case 6:
				$this->setUrl($value);
				break;
			case 7:
				$this->setPubYear($value);
				break;
			case 8:
				$this->setDirector($value);
				break;
			case 9:
				$this->setCast($value);
				break;
			case 10:
				$this->setAbout($value);
				break;
			case 11:
				$this->setCountry($value);
				break;
			case 12:
				$this->setDuration($value);
				break;
			case 13:
				$this->setFileInfo($value);
				break;
			case 14:
				$this->setIsVisible($value);
				break;
			case 15:
				$this->setIsPrivate($value);
				break;
			case 16:
				$this->setIsPublic($value);
				break;
			case 17:
				$this->setModifiedUserId($value);
				break;
			case 18:
				$this->setModifiedAt($value);
				break;
			case 19:
				$this->setModifiedText($value);
				break;
			case 20:
				$this->setCreatedAt($value);
				break;
			case 21:
				$this->setUpdatedAt($value);
				break;
		} // switch()
	}

	/**
	 * Populates the object using an array.
	 *
	 * This is particularly useful when populating an object from one of the
	 * request arrays (e.g. $_POST).  This method goes through the column
	 * names, checking to see whether a matching key exists in populated
	 * array. If so the setByName() method is called for that column.
	 *
	 * You can specify the key type of the array by additionally passing one
	 * of the class type constants BasePeer::TYPE_PHPNAME, BasePeer::TYPE_STUDLYPHPNAME,
	 * BasePeer::TYPE_COLNAME, BasePeer::TYPE_FIELDNAME, BasePeer::TYPE_NUM.
	 * The default key type is the column's phpname (e.g. 'AuthorId')
	 *
	 * @param      array  $arr     An array to populate the object from.
	 * @param      string $keyType The type of keys the array uses.
	 * @return     void
	 */
	public function fromArray($arr, $keyType = BasePeer::TYPE_PHPNAME)
	{
		$keys = FilmPeer::getFieldNames($keyType);

		if (array_key_exists($keys[0], $arr)) $this->setId($arr[$keys[0]]);
		if (array_key_exists($keys[1], $arr)) $this->setUserId($arr[$keys[1]]);
		if (array_key_exists($keys[2], $arr)) $this->setTitle($arr[$keys[2]]);
		if (array_key_exists($keys[3], $arr)) $this->setOriginalTitle($arr[$keys[3]]);
		if (array_key_exists($keys[4], $arr)) $this->setNormalLogo($arr[$keys[4]]);
		if (array_key_exists($keys[5], $arr)) $this->setThumbLogo($arr[$keys[5]]);
		if (array_key_exists($keys[6], $arr)) $this->setUrl($arr[$keys[6]]);
		if (array_key_exists($keys[7], $arr)) $this->setPubYear($arr[$keys[7]]);
		if (array_key_exists($keys[8], $arr)) $this->setDirector($arr[$keys[8]]);
		if (array_key_exists($keys[9], $arr)) $this->setCast($arr[$keys[9]]);
		if (array_key_exists($keys[10], $arr)) $this->setAbout($arr[$keys[10]]);
		if (array_key_exists($keys[11], $arr)) $this->setCountry($arr[$keys[11]]);
		if (array_key_exists($keys[12], $arr)) $this->setDuration($arr[$keys[12]]);
		if (array_key_exists($keys[13], $arr)) $this->setFileInfo($arr[$keys[13]]);
		if (array_key_exists($keys[14], $arr)) $this->setIsVisible($arr[$keys[14]]);
		if (array_key_exists($keys[15], $arr)) $this->setIsPrivate($arr[$keys[15]]);
		if (array_key_exists($keys[16], $arr)) $this->setIsPublic($arr[$keys[16]]);
		if (array_key_exists($keys[17], $arr)) $this->setModifiedUserId($arr[$keys[17]]);
		if (array_key_exists($keys[18], $arr)) $this->setModifiedAt($arr[$keys[18]]);
		if (array_key_exists($keys[19], $arr)) $this->setModifiedText($arr[$keys[19]]);
		if (array_key_exists($keys[20], $arr)) $this->setCreatedAt($arr[$keys[20]]);
		if (array_key_exists($keys[21], $arr)) $this->setUpdatedAt($arr[$keys[21]]);
	}

	/**
	 * Build a Criteria object containing the values of all modified columns in this object.
	 *
	 * @return     Criteria The Criteria object containing all modified values.
	 */
	public function buildCriteria()
	{
		$criteria = new Criteria(FilmPeer::DATABASE_NAME);

		if ($this->isColumnModified(FilmPeer::ID)) $criteria->add(FilmPeer::ID, $this->id);
		if ($this->isColumnModified(FilmPeer::USER_ID)) $criteria->add(FilmPeer::USER_ID, $this->user_id);
		if ($this->isColumnModified(FilmPeer::TITLE)) $criteria->add(FilmPeer::TITLE, $this->title);
		if ($this->isColumnModified(FilmPeer::ORIGINAL_TITLE)) $criteria->add(FilmPeer::ORIGINAL_TITLE, $this->original_title);
		if ($this->isColumnModified(FilmPeer::NORMAL_LOGO)) $criteria->add(FilmPeer::NORMAL_LOGO, $this->normal_logo);
		if ($this->isColumnModified(FilmPeer::THUMB_LOGO)) $criteria->add(FilmPeer::THUMB_LOGO, $this->thumb_logo);
		if ($this->isColumnModified(FilmPeer::URL)) $criteria->add(FilmPeer::URL, $this->url);
		if ($this->isColumnModified(FilmPeer::PUB_YEAR)) $criteria->add(FilmPeer::PUB_YEAR, $this->pub_year);
		if ($this->isColumnModified(FilmPeer::DIRECTOR)) $criteria->add(FilmPeer::DIRECTOR, $this->director);
		if ($this->isColumnModified(FilmPeer::CAST)) $criteria->add(FilmPeer::CAST, $this->cast);
		if ($this->isColumnModified(FilmPeer::ABOUT)) $criteria->add(FilmPeer::ABOUT, $this->about);
		if ($this->isColumnModified(FilmPeer::COUNTRY)) $criteria->add(FilmPeer::COUNTRY, $this->country);
		if ($this->isColumnModified(FilmPeer::DURATION)) $criteria->add(FilmPeer::DURATION, $this->duration);
		if ($this->isColumnModified(FilmPeer::FILE_INFO)) $criteria->add(FilmPeer::FILE_INFO, $this->file_info);
		if ($this->isColumnModified(FilmPeer::IS_VISIBLE)) $criteria->add(FilmPeer::IS_VISIBLE, $this->is_visible);
		if ($this->isColumnModified(FilmPeer::IS_PRIVATE)) $criteria->add(FilmPeer::IS_PRIVATE, $this->is_private);
		if ($this->isColumnModified(FilmPeer::IS_PUBLIC)) $criteria->add(FilmPeer::IS_PUBLIC, $this->is_public);
		if ($this->isColumnModified(FilmPeer::MODIFIED_USER_ID)) $criteria->add(FilmPeer::MODIFIED_USER_ID, $this->modified_user_id);
		if ($this->isColumnModified(FilmPeer::MODIFIED_AT)) $criteria->add(FilmPeer::MODIFIED_AT, $this->modified_at);
		if ($this->isColumnModified(FilmPeer::MODIFIED_TEXT)) $criteria->add(FilmPeer::MODIFIED_TEXT, $this->modified_text);
		if ($this->isColumnModified(FilmPeer::CREATED_AT)) $criteria->add(FilmPeer::CREATED_AT, $this->created_at);
		if ($this->isColumnModified(FilmPeer::UPDATED_AT)) $criteria->add(FilmPeer::UPDATED_AT, $this->updated_at);

		return $criteria;
	}

	/**
	 * Builds a Criteria object containing the primary key for this object.
	 *
	 * Unlike buildCriteria() this method includes the primary key values regardless
	 * of whether or not they have been modified.
	 *
	 * @return     Criteria The Criteria object containing value(s) for primary key(s).
	 */
	public function buildPkeyCriteria()
	{
		$criteria = new Criteria(FilmPeer::DATABASE_NAME);

		$criteria->add(FilmPeer::ID, $this->id);

		return $criteria;
	}

	/**
	 * Returns the primary key for this object (row).
	 * @return     int
	 */
	public function getPrimaryKey()
	{
		return $this->getId();
	}

	/**
	 * Generic method to set the primary key (id column).
	 *
	 * @param      int $key Primary key.
	 * @return     void
	 */
	public function setPrimaryKey($key)
	{
		$this->setId($key);
	}

	/**
	 * Sets contents of passed object to values from current object.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      object $copyObj An object of Film (or compatible) type.
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @throws     PropelException
	 */
	public function copyInto($copyObj, $deepCopy = false)
	{

		$copyObj->setUserId($this->user_id);

		$copyObj->setTitle($this->title);

		$copyObj->setOriginalTitle($this->original_title);

		$copyObj->setNormalLogo($this->normal_logo);

		$copyObj->setThumbLogo($this->thumb_logo);

		$copyObj->setUrl($this->url);

		$copyObj->setPubYear($this->pub_year);

		$copyObj->setDirector($this->director);

		$copyObj->setCast($this->cast);

		$copyObj->setAbout($this->about);

		$copyObj->setCountry($this->country);

		$copyObj->setDuration($this->duration);

		$copyObj->setFileInfo($this->file_info);

		$copyObj->setIsVisible($this->is_visible);

		$copyObj->setIsPrivate($this->is_private);

		$copyObj->setIsPublic($this->is_public);

		$copyObj->setModifiedUserId($this->modified_user_id);

		$copyObj->setModifiedAt($this->modified_at);

		$copyObj->setModifiedText($this->modified_text);

		$copyObj->setCreatedAt($this->created_at);

		$copyObj->setUpdatedAt($this->updated_at);


		if ($deepCopy) {
			// important: temporarily setNew(false) because this affects the behavior of
			// the getter/setter methods for fkey referrer objects.
			$copyObj->setNew(false);

			foreach ($this->getFilmLinkss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFilmLinks($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFilmRaitings() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFilmRaiting($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFilmTotalRatings() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFilmTotalRating($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFilmGallerys() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFilmGallery($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFilmFilmTypess() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFilmFilmTypes($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getFilmTrailers() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addFilmTrailer($relObj->copy($deepCopy));
				}
			}

			foreach ($this->getCommentss() as $relObj) {
				if ($relObj !== $this) {  // ensure that we don't try to copy a reference to ourselves
					$copyObj->addComments($relObj->copy($deepCopy));
				}
			}

		} // if ($deepCopy)


		$copyObj->setNew(true);

		$copyObj->setId(NULL); // this is a auto-increment column, so set to default value

	}

	/**
	 * Makes a copy of this object that will be inserted as a new row in table when saved.
	 * It creates a new object filling in the simple attributes, but skipping any primary
	 * keys that are defined for the table.
	 *
	 * If desired, this method can also make copies of all associated (fkey referrers)
	 * objects.
	 *
	 * @param      boolean $deepCopy Whether to also copy all rows that refer (by fkey) to the current row.
	 * @return     Film Clone of current object.
	 * @throws     PropelException
	 */
	public function copy($deepCopy = false)
	{
		// we use get_class(), because this might be a subclass
		$clazz = get_class($this);
		$copyObj = new $clazz();
		$this->copyInto($copyObj, $deepCopy);
		return $copyObj;
	}

	/**
	 * Returns a peer instance associated with this om.
	 *
	 * Since Peer classes are not to have any instance attributes, this method returns the
	 * same instance for all member of this class. The method could therefore
	 * be static, but this would prevent one from overriding the behavior.
	 *
	 * @return     FilmPeer
	 */
	public function getPeer()
	{
		if (self::$peer === null) {
			self::$peer = new FilmPeer();
		}
		return self::$peer;
	}

	/**
	 * Declares an association between this object and a Users object.
	 *
	 * @param      Users $v
	 * @return     Film The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUsersRelatedByUserId(Users $v = null)
	{
		if ($v === null) {
			$this->setUserId(NULL);
		} else {
			$this->setUserId($v->getId());
		}

		$this->aUsersRelatedByUserId = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Users object, it will not be re-added.
		if ($v !== null) {
			$v->addFilmRelatedByUserId($this);
		}

		return $this;
	}


	/**
	 * Get the associated Users object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Users The associated Users object.
	 * @throws     PropelException
	 */
	public function getUsersRelatedByUserId(PropelPDO $con = null)
	{
		if ($this->aUsersRelatedByUserId === null && ($this->user_id !== null)) {
			$this->aUsersRelatedByUserId = UsersPeer::retrieveByPk($this->user_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUsersRelatedByUserId->addFilmsRelatedByUserId($this);
			 */
		}
		return $this->aUsersRelatedByUserId;
	}

	/**
	 * Declares an association between this object and a Users object.
	 *
	 * @param      Users $v
	 * @return     Film The current object (for fluent API support)
	 * @throws     PropelException
	 */
	public function setUsersRelatedByModifiedUserId(Users $v = null)
	{
		if ($v === null) {
			$this->setModifiedUserId(NULL);
		} else {
			$this->setModifiedUserId($v->getId());
		}

		$this->aUsersRelatedByModifiedUserId = $v;

		// Add binding for other direction of this n:n relationship.
		// If this object has already been added to the Users object, it will not be re-added.
		if ($v !== null) {
			$v->addFilmRelatedByModifiedUserId($this);
		}

		return $this;
	}


	/**
	 * Get the associated Users object
	 *
	 * @param      PropelPDO Optional Connection object.
	 * @return     Users The associated Users object.
	 * @throws     PropelException
	 */
	public function getUsersRelatedByModifiedUserId(PropelPDO $con = null)
	{
		if ($this->aUsersRelatedByModifiedUserId === null && ($this->modified_user_id !== null)) {
			$this->aUsersRelatedByModifiedUserId = UsersPeer::retrieveByPk($this->modified_user_id);
			/* The following can be used additionally to
			   guarantee the related object contains a reference
			   to this object.  This level of coupling may, however, be
			   undesirable since it could result in an only partially populated collection
			   in the referenced object.
			   $this->aUsersRelatedByModifiedUserId->addFilmsRelatedByModifiedUserId($this);
			 */
		}
		return $this->aUsersRelatedByModifiedUserId;
	}

	/**
	 * Clears out the collFilmLinkss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFilmLinkss()
	 */
	public function clearFilmLinkss()
	{
		$this->collFilmLinkss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFilmLinkss collection (array).
	 *
	 * By default this just sets the collFilmLinkss collection to an empty array (like clearcollFilmLinkss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFilmLinkss()
	{
		$this->collFilmLinkss = array();
	}

	/**
	 * Gets an array of FilmLinks objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Film has previously been saved, it will retrieve
	 * related FilmLinkss from storage. If this Film is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array FilmLinks[]
	 * @throws     PropelException
	 */
	public function getFilmLinkss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFilmLinkss === null) {
			if ($this->isNew()) {
			   $this->collFilmLinkss = array();
			} else {

				$criteria->add(FilmLinksPeer::FILM_ID, $this->id);

				FilmLinksPeer::addSelectColumns($criteria);
				$this->collFilmLinkss = FilmLinksPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FilmLinksPeer::FILM_ID, $this->id);

				FilmLinksPeer::addSelectColumns($criteria);
				if (!isset($this->lastFilmLinksCriteria) || !$this->lastFilmLinksCriteria->equals($criteria)) {
					$this->collFilmLinkss = FilmLinksPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFilmLinksCriteria = $criteria;
		return $this->collFilmLinkss;
	}

	/**
	 * Returns the number of related FilmLinks objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related FilmLinks objects.
	 * @throws     PropelException
	 */
	public function countFilmLinkss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFilmLinkss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FilmLinksPeer::FILM_ID, $this->id);

				$count = FilmLinksPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FilmLinksPeer::FILM_ID, $this->id);

				if (!isset($this->lastFilmLinksCriteria) || !$this->lastFilmLinksCriteria->equals($criteria)) {
					$count = FilmLinksPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFilmLinkss);
				}
			} else {
				$count = count($this->collFilmLinkss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a FilmLinks object to this object
	 * through the FilmLinks foreign key attribute.
	 *
	 * @param      FilmLinks $l FilmLinks
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFilmLinks(FilmLinks $l)
	{
		if ($this->collFilmLinkss === null) {
			$this->initFilmLinkss();
		}
		if (!in_array($l, $this->collFilmLinkss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFilmLinkss, $l);
			$l->setFilm($this);
		}
	}

	/**
	 * Clears out the collFilmRaitings collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFilmRaitings()
	 */
	public function clearFilmRaitings()
	{
		$this->collFilmRaitings = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFilmRaitings collection (array).
	 *
	 * By default this just sets the collFilmRaitings collection to an empty array (like clearcollFilmRaitings());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFilmRaitings()
	{
		$this->collFilmRaitings = array();
	}

	/**
	 * Gets an array of FilmRaiting objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Film has previously been saved, it will retrieve
	 * related FilmRaitings from storage. If this Film is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array FilmRaiting[]
	 * @throws     PropelException
	 */
	public function getFilmRaitings($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFilmRaitings === null) {
			if ($this->isNew()) {
			   $this->collFilmRaitings = array();
			} else {

				$criteria->add(FilmRaitingPeer::FILM_ID, $this->id);

				FilmRaitingPeer::addSelectColumns($criteria);
				$this->collFilmRaitings = FilmRaitingPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FilmRaitingPeer::FILM_ID, $this->id);

				FilmRaitingPeer::addSelectColumns($criteria);
				if (!isset($this->lastFilmRaitingCriteria) || !$this->lastFilmRaitingCriteria->equals($criteria)) {
					$this->collFilmRaitings = FilmRaitingPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFilmRaitingCriteria = $criteria;
		return $this->collFilmRaitings;
	}

	/**
	 * Returns the number of related FilmRaiting objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related FilmRaiting objects.
	 * @throws     PropelException
	 */
	public function countFilmRaitings(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFilmRaitings === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FilmRaitingPeer::FILM_ID, $this->id);

				$count = FilmRaitingPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FilmRaitingPeer::FILM_ID, $this->id);

				if (!isset($this->lastFilmRaitingCriteria) || !$this->lastFilmRaitingCriteria->equals($criteria)) {
					$count = FilmRaitingPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFilmRaitings);
				}
			} else {
				$count = count($this->collFilmRaitings);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a FilmRaiting object to this object
	 * through the FilmRaiting foreign key attribute.
	 *
	 * @param      FilmRaiting $l FilmRaiting
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFilmRaiting(FilmRaiting $l)
	{
		if ($this->collFilmRaitings === null) {
			$this->initFilmRaitings();
		}
		if (!in_array($l, $this->collFilmRaitings, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFilmRaitings, $l);
			$l->setFilm($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Film is new, it will return
	 * an empty collection; or if this Film has previously
	 * been saved, it will retrieve related FilmRaitings from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Film.
	 */
	public function getFilmRaitingsJoinUsers($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFilmRaitings === null) {
			if ($this->isNew()) {
				$this->collFilmRaitings = array();
			} else {

				$criteria->add(FilmRaitingPeer::FILM_ID, $this->id);

				$this->collFilmRaitings = FilmRaitingPeer::doSelectJoinUsers($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilmRaitingPeer::FILM_ID, $this->id);

			if (!isset($this->lastFilmRaitingCriteria) || !$this->lastFilmRaitingCriteria->equals($criteria)) {
				$this->collFilmRaitings = FilmRaitingPeer::doSelectJoinUsers($criteria, $con, $join_behavior);
			}
		}
		$this->lastFilmRaitingCriteria = $criteria;

		return $this->collFilmRaitings;
	}

	/**
	 * Clears out the collFilmTotalRatings collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFilmTotalRatings()
	 */
	public function clearFilmTotalRatings()
	{
		$this->collFilmTotalRatings = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFilmTotalRatings collection (array).
	 *
	 * By default this just sets the collFilmTotalRatings collection to an empty array (like clearcollFilmTotalRatings());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFilmTotalRatings()
	{
		$this->collFilmTotalRatings = array();
	}

	/**
	 * Gets an array of FilmTotalRating objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Film has previously been saved, it will retrieve
	 * related FilmTotalRatings from storage. If this Film is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array FilmTotalRating[]
	 * @throws     PropelException
	 */
	public function getFilmTotalRatings($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFilmTotalRatings === null) {
			if ($this->isNew()) {
			   $this->collFilmTotalRatings = array();
			} else {

				$criteria->add(FilmTotalRatingPeer::FILM_ID, $this->id);

				FilmTotalRatingPeer::addSelectColumns($criteria);
				$this->collFilmTotalRatings = FilmTotalRatingPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FilmTotalRatingPeer::FILM_ID, $this->id);

				FilmTotalRatingPeer::addSelectColumns($criteria);
				if (!isset($this->lastFilmTotalRatingCriteria) || !$this->lastFilmTotalRatingCriteria->equals($criteria)) {
					$this->collFilmTotalRatings = FilmTotalRatingPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFilmTotalRatingCriteria = $criteria;
		return $this->collFilmTotalRatings;
	}

	/**
	 * Returns the number of related FilmTotalRating objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related FilmTotalRating objects.
	 * @throws     PropelException
	 */
	public function countFilmTotalRatings(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFilmTotalRatings === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FilmTotalRatingPeer::FILM_ID, $this->id);

				$count = FilmTotalRatingPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FilmTotalRatingPeer::FILM_ID, $this->id);

				if (!isset($this->lastFilmTotalRatingCriteria) || !$this->lastFilmTotalRatingCriteria->equals($criteria)) {
					$count = FilmTotalRatingPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFilmTotalRatings);
				}
			} else {
				$count = count($this->collFilmTotalRatings);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a FilmTotalRating object to this object
	 * through the FilmTotalRating foreign key attribute.
	 *
	 * @param      FilmTotalRating $l FilmTotalRating
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFilmTotalRating(FilmTotalRating $l)
	{
		if ($this->collFilmTotalRatings === null) {
			$this->initFilmTotalRatings();
		}
		if (!in_array($l, $this->collFilmTotalRatings, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFilmTotalRatings, $l);
			$l->setFilm($this);
		}
	}

	/**
	 * Clears out the collFilmGallerys collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFilmGallerys()
	 */
	public function clearFilmGallerys()
	{
		$this->collFilmGallerys = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFilmGallerys collection (array).
	 *
	 * By default this just sets the collFilmGallerys collection to an empty array (like clearcollFilmGallerys());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFilmGallerys()
	{
		$this->collFilmGallerys = array();
	}

	/**
	 * Gets an array of FilmGallery objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Film has previously been saved, it will retrieve
	 * related FilmGallerys from storage. If this Film is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array FilmGallery[]
	 * @throws     PropelException
	 */
	public function getFilmGallerys($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFilmGallerys === null) {
			if ($this->isNew()) {
			   $this->collFilmGallerys = array();
			} else {

				$criteria->add(FilmGalleryPeer::FILM_ID, $this->id);

				FilmGalleryPeer::addSelectColumns($criteria);
				$this->collFilmGallerys = FilmGalleryPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FilmGalleryPeer::FILM_ID, $this->id);

				FilmGalleryPeer::addSelectColumns($criteria);
				if (!isset($this->lastFilmGalleryCriteria) || !$this->lastFilmGalleryCriteria->equals($criteria)) {
					$this->collFilmGallerys = FilmGalleryPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFilmGalleryCriteria = $criteria;
		return $this->collFilmGallerys;
	}

	/**
	 * Returns the number of related FilmGallery objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related FilmGallery objects.
	 * @throws     PropelException
	 */
	public function countFilmGallerys(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFilmGallerys === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FilmGalleryPeer::FILM_ID, $this->id);

				$count = FilmGalleryPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FilmGalleryPeer::FILM_ID, $this->id);

				if (!isset($this->lastFilmGalleryCriteria) || !$this->lastFilmGalleryCriteria->equals($criteria)) {
					$count = FilmGalleryPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFilmGallerys);
				}
			} else {
				$count = count($this->collFilmGallerys);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a FilmGallery object to this object
	 * through the FilmGallery foreign key attribute.
	 *
	 * @param      FilmGallery $l FilmGallery
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFilmGallery(FilmGallery $l)
	{
		if ($this->collFilmGallerys === null) {
			$this->initFilmGallerys();
		}
		if (!in_array($l, $this->collFilmGallerys, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFilmGallerys, $l);
			$l->setFilm($this);
		}
	}

	/**
	 * Clears out the collFilmFilmTypess collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFilmFilmTypess()
	 */
	public function clearFilmFilmTypess()
	{
		$this->collFilmFilmTypess = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFilmFilmTypess collection (array).
	 *
	 * By default this just sets the collFilmFilmTypess collection to an empty array (like clearcollFilmFilmTypess());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFilmFilmTypess()
	{
		$this->collFilmFilmTypess = array();
	}

	/**
	 * Gets an array of FilmFilmTypes objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Film has previously been saved, it will retrieve
	 * related FilmFilmTypess from storage. If this Film is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array FilmFilmTypes[]
	 * @throws     PropelException
	 */
	public function getFilmFilmTypess($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFilmFilmTypess === null) {
			if ($this->isNew()) {
			   $this->collFilmFilmTypess = array();
			} else {

				$criteria->add(FilmFilmTypesPeer::FILM_ID, $this->id);

				FilmFilmTypesPeer::addSelectColumns($criteria);
				$this->collFilmFilmTypess = FilmFilmTypesPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FilmFilmTypesPeer::FILM_ID, $this->id);

				FilmFilmTypesPeer::addSelectColumns($criteria);
				if (!isset($this->lastFilmFilmTypesCriteria) || !$this->lastFilmFilmTypesCriteria->equals($criteria)) {
					$this->collFilmFilmTypess = FilmFilmTypesPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFilmFilmTypesCriteria = $criteria;
		return $this->collFilmFilmTypess;
	}

	/**
	 * Returns the number of related FilmFilmTypes objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related FilmFilmTypes objects.
	 * @throws     PropelException
	 */
	public function countFilmFilmTypess(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFilmFilmTypess === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FilmFilmTypesPeer::FILM_ID, $this->id);

				$count = FilmFilmTypesPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FilmFilmTypesPeer::FILM_ID, $this->id);

				if (!isset($this->lastFilmFilmTypesCriteria) || !$this->lastFilmFilmTypesCriteria->equals($criteria)) {
					$count = FilmFilmTypesPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFilmFilmTypess);
				}
			} else {
				$count = count($this->collFilmFilmTypess);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a FilmFilmTypes object to this object
	 * through the FilmFilmTypes foreign key attribute.
	 *
	 * @param      FilmFilmTypes $l FilmFilmTypes
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFilmFilmTypes(FilmFilmTypes $l)
	{
		if ($this->collFilmFilmTypess === null) {
			$this->initFilmFilmTypess();
		}
		if (!in_array($l, $this->collFilmFilmTypess, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFilmFilmTypess, $l);
			$l->setFilm($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Film is new, it will return
	 * an empty collection; or if this Film has previously
	 * been saved, it will retrieve related FilmFilmTypess from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Film.
	 */
	public function getFilmFilmTypessJoinFilmTypes($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFilmFilmTypess === null) {
			if ($this->isNew()) {
				$this->collFilmFilmTypess = array();
			} else {

				$criteria->add(FilmFilmTypesPeer::FILM_ID, $this->id);

				$this->collFilmFilmTypess = FilmFilmTypesPeer::doSelectJoinFilmTypes($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(FilmFilmTypesPeer::FILM_ID, $this->id);

			if (!isset($this->lastFilmFilmTypesCriteria) || !$this->lastFilmFilmTypesCriteria->equals($criteria)) {
				$this->collFilmFilmTypess = FilmFilmTypesPeer::doSelectJoinFilmTypes($criteria, $con, $join_behavior);
			}
		}
		$this->lastFilmFilmTypesCriteria = $criteria;

		return $this->collFilmFilmTypess;
	}

	/**
	 * Clears out the collFilmTrailers collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addFilmTrailers()
	 */
	public function clearFilmTrailers()
	{
		$this->collFilmTrailers = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collFilmTrailers collection (array).
	 *
	 * By default this just sets the collFilmTrailers collection to an empty array (like clearcollFilmTrailers());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initFilmTrailers()
	{
		$this->collFilmTrailers = array();
	}

	/**
	 * Gets an array of FilmTrailer objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Film has previously been saved, it will retrieve
	 * related FilmTrailers from storage. If this Film is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array FilmTrailer[]
	 * @throws     PropelException
	 */
	public function getFilmTrailers($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collFilmTrailers === null) {
			if ($this->isNew()) {
			   $this->collFilmTrailers = array();
			} else {

				$criteria->add(FilmTrailerPeer::FILM_ID, $this->id);

				FilmTrailerPeer::addSelectColumns($criteria);
				$this->collFilmTrailers = FilmTrailerPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(FilmTrailerPeer::FILM_ID, $this->id);

				FilmTrailerPeer::addSelectColumns($criteria);
				if (!isset($this->lastFilmTrailerCriteria) || !$this->lastFilmTrailerCriteria->equals($criteria)) {
					$this->collFilmTrailers = FilmTrailerPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastFilmTrailerCriteria = $criteria;
		return $this->collFilmTrailers;
	}

	/**
	 * Returns the number of related FilmTrailer objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related FilmTrailer objects.
	 * @throws     PropelException
	 */
	public function countFilmTrailers(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collFilmTrailers === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(FilmTrailerPeer::FILM_ID, $this->id);

				$count = FilmTrailerPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(FilmTrailerPeer::FILM_ID, $this->id);

				if (!isset($this->lastFilmTrailerCriteria) || !$this->lastFilmTrailerCriteria->equals($criteria)) {
					$count = FilmTrailerPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collFilmTrailers);
				}
			} else {
				$count = count($this->collFilmTrailers);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a FilmTrailer object to this object
	 * through the FilmTrailer foreign key attribute.
	 *
	 * @param      FilmTrailer $l FilmTrailer
	 * @return     void
	 * @throws     PropelException
	 */
	public function addFilmTrailer(FilmTrailer $l)
	{
		if ($this->collFilmTrailers === null) {
			$this->initFilmTrailers();
		}
		if (!in_array($l, $this->collFilmTrailers, true)) { // only add it if the **same** object is not already associated
			array_push($this->collFilmTrailers, $l);
			$l->setFilm($this);
		}
	}

	/**
	 * Clears out the collCommentss collection (array).
	 *
	 * This does not modify the database; however, it will remove any associated objects, causing
	 * them to be refetched by subsequent calls to accessor method.
	 *
	 * @return     void
	 * @see        addCommentss()
	 */
	public function clearCommentss()
	{
		$this->collCommentss = null; // important to set this to NULL since that means it is uninitialized
	}

	/**
	 * Initializes the collCommentss collection (array).
	 *
	 * By default this just sets the collCommentss collection to an empty array (like clearcollCommentss());
	 * however, you may wish to override this method in your stub class to provide setting appropriate
	 * to your application -- for example, setting the initial array to the values stored in database.
	 *
	 * @return     void
	 */
	public function initCommentss()
	{
		$this->collCommentss = array();
	}

	/**
	 * Gets an array of Comments objects which contain a foreign key that references this object.
	 *
	 * If this collection has already been initialized with an identical Criteria, it returns the collection.
	 * Otherwise if this Film has previously been saved, it will retrieve
	 * related Commentss from storage. If this Film is new, it will return
	 * an empty collection or the current collection, the criteria is ignored on a new object.
	 *
	 * @param      PropelPDO $con
	 * @param      Criteria $criteria
	 * @return     array Comments[]
	 * @throws     PropelException
	 */
	public function getCommentss($criteria = null, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCommentss === null) {
			if ($this->isNew()) {
			   $this->collCommentss = array();
			} else {

				$criteria->add(CommentsPeer::FILM_ID, $this->id);

				CommentsPeer::addSelectColumns($criteria);
				$this->collCommentss = CommentsPeer::doSelect($criteria, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return the collection.


				$criteria->add(CommentsPeer::FILM_ID, $this->id);

				CommentsPeer::addSelectColumns($criteria);
				if (!isset($this->lastCommentsCriteria) || !$this->lastCommentsCriteria->equals($criteria)) {
					$this->collCommentss = CommentsPeer::doSelect($criteria, $con);
				}
			}
		}
		$this->lastCommentsCriteria = $criteria;
		return $this->collCommentss;
	}

	/**
	 * Returns the number of related Comments objects.
	 *
	 * @param      Criteria $criteria
	 * @param      boolean $distinct
	 * @param      PropelPDO $con
	 * @return     int Count of related Comments objects.
	 * @throws     PropelException
	 */
	public function countCommentss(Criteria $criteria = null, $distinct = false, PropelPDO $con = null)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		} else {
			$criteria = clone $criteria;
		}

		if ($distinct) {
			$criteria->setDistinct();
		}

		$count = null;

		if ($this->collCommentss === null) {
			if ($this->isNew()) {
				$count = 0;
			} else {

				$criteria->add(CommentsPeer::FILM_ID, $this->id);

				$count = CommentsPeer::doCount($criteria, false, $con);
			}
		} else {
			// criteria has no effect for a new object
			if (!$this->isNew()) {
				// the following code is to determine if a new query is
				// called for.  If the criteria is the same as the last
				// one, just return count of the collection.


				$criteria->add(CommentsPeer::FILM_ID, $this->id);

				if (!isset($this->lastCommentsCriteria) || !$this->lastCommentsCriteria->equals($criteria)) {
					$count = CommentsPeer::doCount($criteria, false, $con);
				} else {
					$count = count($this->collCommentss);
				}
			} else {
				$count = count($this->collCommentss);
			}
		}
		return $count;
	}

	/**
	 * Method called to associate a Comments object to this object
	 * through the Comments foreign key attribute.
	 *
	 * @param      Comments $l Comments
	 * @return     void
	 * @throws     PropelException
	 */
	public function addComments(Comments $l)
	{
		if ($this->collCommentss === null) {
			$this->initCommentss();
		}
		if (!in_array($l, $this->collCommentss, true)) { // only add it if the **same** object is not already associated
			array_push($this->collCommentss, $l);
			$l->setFilm($this);
		}
	}


	/**
	 * If this collection has already been initialized with
	 * an identical criteria, it returns the collection.
	 * Otherwise if this Film is new, it will return
	 * an empty collection; or if this Film has previously
	 * been saved, it will retrieve related Commentss from storage.
	 *
	 * This method is protected by default in order to keep the public
	 * api reasonable.  You can provide public methods for those you
	 * actually need in Film.
	 */
	public function getCommentssJoinUsers($criteria = null, $con = null, $join_behavior = Criteria::LEFT_JOIN)
	{
		if ($criteria === null) {
			$criteria = new Criteria(FilmPeer::DATABASE_NAME);
		}
		elseif ($criteria instanceof Criteria)
		{
			$criteria = clone $criteria;
		}

		if ($this->collCommentss === null) {
			if ($this->isNew()) {
				$this->collCommentss = array();
			} else {

				$criteria->add(CommentsPeer::FILM_ID, $this->id);

				$this->collCommentss = CommentsPeer::doSelectJoinUsers($criteria, $con, $join_behavior);
			}
		} else {
			// the following code is to determine if a new query is
			// called for.  If the criteria is the same as the last
			// one, just return the collection.

			$criteria->add(CommentsPeer::FILM_ID, $this->id);

			if (!isset($this->lastCommentsCriteria) || !$this->lastCommentsCriteria->equals($criteria)) {
				$this->collCommentss = CommentsPeer::doSelectJoinUsers($criteria, $con, $join_behavior);
			}
		}
		$this->lastCommentsCriteria = $criteria;

		return $this->collCommentss;
	}

	/**
	 * Resets all collections of referencing foreign keys.
	 *
	 * This method is a user-space workaround for PHP's inability to garbage collect objects
	 * with circular references.  This is currently necessary when using Propel in certain
	 * daemon or large-volumne/high-memory operations.
	 *
	 * @param      boolean $deep Whether to also clear the references on all associated objects.
	 */
	public function clearAllReferences($deep = false)
	{
		if ($deep) {
			if ($this->collFilmLinkss) {
				foreach ((array) $this->collFilmLinkss as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFilmRaitings) {
				foreach ((array) $this->collFilmRaitings as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFilmTotalRatings) {
				foreach ((array) $this->collFilmTotalRatings as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFilmGallerys) {
				foreach ((array) $this->collFilmGallerys as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFilmFilmTypess) {
				foreach ((array) $this->collFilmFilmTypess as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collFilmTrailers) {
				foreach ((array) $this->collFilmTrailers as $o) {
					$o->clearAllReferences($deep);
				}
			}
			if ($this->collCommentss) {
				foreach ((array) $this->collCommentss as $o) {
					$o->clearAllReferences($deep);
				}
			}
		} // if ($deep)

		$this->collFilmLinkss = null;
		$this->collFilmRaitings = null;
		$this->collFilmTotalRatings = null;
		$this->collFilmGallerys = null;
		$this->collFilmFilmTypess = null;
		$this->collFilmTrailers = null;
		$this->collCommentss = null;
			$this->aUsersRelatedByUserId = null;
			$this->aUsersRelatedByModifiedUserId = null;
	}

	// symfony_behaviors behavior
	
	/**
	 * Calls methods defined via {@link sfMixer}.
	 */
	public function __call($method, $arguments)
	{
	  if (!$callable = sfMixer::getCallable('BaseFilm:'.$method))
	  {
	    throw new sfException(sprintf('Call to undefined method BaseFilm::%s', $method));
	  }
	
	  array_unshift($arguments, $this);
	
	  return call_user_func_array($callable, $arguments);
	}

} // BaseFilm
